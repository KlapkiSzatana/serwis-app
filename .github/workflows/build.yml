name: Build & Release Serwis App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: SerwisApp_Setup.exe
          - os: macos-14
            artifact_name: SerwisApp

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PySide6 cryptography pillow requests python-barcode packaging pyinstaller

      - name: Build Executable
        shell: bash
        run: |
          if [ "${{ matrix.os }}" == "windows-latest" ]; then
            # 1. Budowanie Windows - tryb Standalone (katalog plików)
            python -m PyInstaller --noconfirm --windowed \
              --add-data "serwisapp.png;." \
              --add-data "icon.png;." \
              --add-data "actions;actions" \
              --add-data "fonts;fonts" \
              --add-data "modules;modules" \
              --add-data "setup;setup" \
              --add-data "ui;ui" \
              --add-data "resources;resources" \
              --version-file "file_version_info.txt" \
              --icon "serwisapp.ico" \
              --name "serwis-app" \
              serwis-app.py

            # 2. Tworzenie instalatora za pomocą Inno Setup
            "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

            # 3. Czyszczenie katalogów dla Windows
            mkdir -p dist
            mv user_setup/SerwisApp_Setup.exe dist/SerwisApp_Setup.exe
            rm -rf dist/serwis-app/
            rm -f dist/serwis-app.exe
            rm -f dist/serwis-app-windows.exe
          else
            # 4. Dla macOS - zachowujemy tryb pojedynczego pliku
            python -m PyInstaller --noconfirm --onefile --windowed \
              --add-data "serwisapp.png:." \
              --add-data "icon.png:." \
              --add-data "actions:actions" \
              --add-data "fonts:fonts" \
              --add-data "modules:modules" \
              --add-data "setup:setup" \
              --add-data "ui:ui" \
              --add-data "resources:resources" \
              --osx-bundle-identifier "com.klapkiszatana.serwis-app" \
              --name "SerwisApp" \
              serwis-app.py

            mv dist/SerwisApp dist/SerwisApp_macos
          fi

      - name: Create Release and Upload
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            dist/SerwisApp_Setup.exe
            dist/SerwisApp_macos
          body: |
            ## Zmiany w tej wersji:
            - Poprawiono instalator (dodano więcej informacji) i ikonki dla Windowsa.
